[project]
name = "collector"
version = "0.1.0"
description = "GitHub Star Data Collector"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
